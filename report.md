# D1 每日作业报告

> 这是本次 Day 1 的提交版报告草稿。里面记录的命令、指标和案例都来自当前工作副本的真实运行结果。

## 1. 本日问题

- 里程碑：day-01
- 学生或小组：待填写
- 使用者：准备历史展览、希望解释数据和模型局限的博物馆教育团队
- 真实输入：Kaggle Titanic Dataset 的 `train.csv`，891 行、12 列真实乘客记录
- 需要的输出：在同一留出测试集上比较多数类基线与随机森林候选模型，并解释真实错误样本
- 与使用者最相关的错误：假阴性和假阳性，尤其是真实幸存者被漏掉或真实未幸存者被误判的情况
- 本日产品边界：这是历史观察数据分析，不是现代救援分配工具，也不能证明某个特征导致生存

## 2. 真实数据或真实课程输入

- 所有者/发布者：Kaggle / hesh97
- 标题：Titanic-Dataset (train.csv)
- 原始 URL：https://www.kaggle.com/datasets/hesh97/titanicdataset-traincsv
- 许可标签或使用许可：CC0: Public Domain
- 下载/取得日期：2026-08-17
- 预期文件与结构：`data/raw/train.csv`
- 检查命令：`py train.py --check-data`
- 实际检查结果：`REAL DATA CHECK PASSED`；`rows: 891`；`survived_counts: {0: 549, 1: 342}`
- 已知缺失、偏差或限制：`Age` 有 177 个缺失值，`Embarked` 有 2 个缺失值，`Cabin` 大量缺失；这些只能在当前数据里解释，不能外推成因果结论

## 3. 可复现运行

```powershell
# 当前目录
Get-Location

# 安装
py -m pip install -r requirements.txt

# 数据检查
py train.py --check-data

# 主程序
py train.py

# 测试
py -m unittest discover -s tests -v
```

关键输出文件：`metrics.json`、`errors.csv`

## 4. 基线与候选

### 简单基线

- 方法：`DummyClassifier(strategy="most_frequent")`
- 为什么足够简单：它永远猜训练集中最常见的类别，是最容易理解和复查的最低比较标准
- 命令：`py train.py`
- 结果：
  - accuracy `0.6143497757847534`
  - precision_survived `0.0`
  - recall_survived `0.0`
  - f1_survived `0.0`
  - confusion matrix `[[137, 0], [86, 0]]`

### 候选方法

- 学生完成的核心改动：在现有预处理后加入 `RandomForestClassifier(random_state=42, n_estimators=200)`
- 保持不变的数据、划分、指标或参数：真实 Titanic `train.csv`、固定 75/25 划分、同一预处理、同一评估指标、同一测试集
- 命令：`py train.py`
- 结果：
  - accuracy `0.7533632286995515`
  - precision_survived `0.6962025316455697`
  - recall_survived `0.6395348837209303`
  - f1_survived `0.6666666666666666`
  - confusion matrix `[[113, 24], [31, 55]]`

| 项目 | 基线 | 候选 | 含义 |
| --- | ---: | ---: | --- |
| 主指标 | 0.6143497757847534 | 0.7533632286995515 | 候选模型在同一测试集上的总体分类效果更好 |
| 重要错误 | 只会预测多数类，导致对幸存者的召回率为 0 | 仍会错分，但能识别出一部分真实幸存者 | 候选模型比基线更有信息量 |

## 5. 一个真实失败案例

- 样本位置/编号：`source_row 501`，`PassengerId 502`
- 真实结果：`true_survived = 0`
- 系统输出：`predicted_survived = 1`
- 可以观察到什么：`Pclass=3`、`Sex=female`、`Age=21`、`SibSp=0`、`Parch=0`、`Fare=7.75`、`Embarked=Q`
- 说明的限制：模型在某些第三舱样本上仍会误判；它不是对每个乘客都稳定正确
- 不能证明什么：不能证明性别、舱位或票价导致了生存或死亡，也不能用这一个样本代表全部乘客
- 下一项最小检查：继续比较更多错分样本，看看错误是否集中在某些特征组合上，而不是只看一个样本

## 6. 智能体与学生工作边界

- 智能体提出/生成/修改了什么：建议了报告结构，帮助补上候选模型的代码，并整理了结果解释
- 学生怎样核对文件、来源、输出、测试和 diff：通过真实数据检查、运行 `py train.py`、查看 `metrics.json` / `errors.csv`、核对 `train.py` 中的模型实现
- 学生修改或拒绝了什么建议：保留固定划分、固定预处理和固定随机种子，没有为了好看改测试集或改指标
- 每名成员能独立解释的代码或证据：数据检查逻辑、基线模型、候选随机森林、混淆矩阵、一个真实错误案例

## 6.1 你如果要直接复述给老师

- 我们先确认数据是真的，再比较多数类基线和随机森林候选模型。
- 候选模型在同一测试集上的 accuracy、recall 和 F1 都高于基线。
- 我们还打开了真实错分样本，说明模型仍然会错。
- 报告里的结论只限于这份历史数据，不能推出因果。

## 7. 结论与限制

这次实验在同一份真实 Titanic 数据上，比较了一个多数类基线和一个固定随机种子的随机森林候选模型。结果显示，候选模型的 accuracy、recall 和 F1 都明显高于基线，说明它在这份历史数据上的预测能力更强。与此同时，候选模型仍然会产生真实错分样本，说明它不是万能的。一个具体失败案例是 `PassengerId 502`，模型把真实未幸存者误判成幸存者，这可以说明模型会漏掉某些样本，但不能推出因果关系。数据本身有缺失值，因此预处理是必要的，但预处理不等于因果解释。这个结果只支持“在本次固定划分与固定预处理下，候选模型优于基线”这一最小结论，不能被拿去做现代救援决策。

## 8. 提交复核

- [ ] README 从新环境可以开始运行
- [ ] 数据检查、测试和主程序重新运行
- [ ] 报告数字与保存输出一致
- [ ] `presentation.pptx` 在 3 分钟内讲完
- [ ] `submission.json` 路径正确
- [ ] 无密钥、大数据、私人信息、虚拟环境或缓存
- [ ] GitHub 网页复查并邮件发送 URL